[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fastapi-gen"
version = "0.1.0"
description = "CLI tool to scaffold FastAPI applications"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fastapi-gen = "fastapi_gen:main"
